define(['property', 'model', 'collection', 'databind'], function (Property, Model, Collection, Binding) {

	var JPF = {
		Binding: Binding,
		Collection: Collection,
		Model: Model,
		Property: Property
	};

	return JPF;

});